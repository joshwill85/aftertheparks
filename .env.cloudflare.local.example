# Cloudflare infra credentials (gitignored). Copy to .env.cloudflare.local
# Create token: npm run bootstrap:turnstile (opens pre-filled dashboard page)
# Required permission: Turnstile Sites Write
CLOUDFLARE_API_TOKEN=
